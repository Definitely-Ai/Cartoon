# Rick presentation review - September 15, 2026

## Scope

Reviewed the public Presentation tab, ten-slide PowerPoint and matching PDF.
The 40 approved cartoons, cast canon, captions and existing print sizes are unchanged.
The September 14 downloads remain available at their original URLs. New downloads
use `/gallery/presentation-20260915/` so an older cached deck cannot replace this revision.

## Changes

- Four featured cartoon slides now repeat their exact caption as large editable
  text beside the unmodified original artwork. Georgia, the ivory/green design,
  slide dimensions and ten-slide structure remain intact.
- The date and automation explanation reflect the current release. Selected
  Austin and Los Angeles examples are explicitly distinguished from a new live run.
- Packet selection has thumbnails, clear/reset controls, separate accessible
  preview buttons and consistent titles. Download feedback clears when choices change.
- Slideshow keyboard controls contain focus, return focus on exit and prevent
  background scrolling when a browser denies native full-screen mode.

## Verification

- `scripts/verify-rick-presentation-browser.mjs`: 40 full-resolution originals,
  four sizes on Letter and A4, six-page newspaper proof, selection limit, keyboard
  wraparound, Escape/focus restoration, denied-fullscreen fallback, mobile fit,
  exact PDF/PPTX download hashes and no browser page errors.
- `scripts/check-rick-showcase.py`: ten pages/slides, seven exact approved PNGs
  embedded in PPTX, native-resolution PDF images, four exact featured captions,
  editable print table, required text and PDF text bounds.
- All ten PPTX renders and ten PDF pages visually inspected. The newspaper proof
  and large A4 proof retain the complete cartoon and its caption.
- Twelve existing artwork/cast/print regression tests pass. TypeScript and the
  direct Next.js production build pass. The release's normal prebuild continues
  to run on Vercel; local archive indexing was not rerun for this UI-only change.

Private render and browser records: `output/presentation-review-20260915/`.
PPTX validation receipt: `output/rick-showcase-build/presentation-review-20260915/validation.json`.

## Automation boundary

The installed `SwingingDoor-CartoonWorker` task was read without changing it.
It still targets `20260914-v14`. Corrected-cast runtime `20260914-v17` remains
staged, not activated. No GPU jobs, schedules, authentication settings or database
records were changed by this presentation review. This review does not claim a
new end-to-end unattended generation or a power-loss test.

The site and slides explicitly state the outstanding worker activation. Update
that dated rollout note after activation and a successful real generation test.
