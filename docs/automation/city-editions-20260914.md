# Austin and Los Angeles — September 14, 2026

Two editorially reviewed city cartoons were prepared for explicit owner-requested publication above the original 38. The original manifest, originals and 38-image ZIP remain unchanged. The city images have individual downloads and primary-source context links.

## What ran

- Local runtime `20260914-v10`: operator-configured primary article retrieval, local caption writer, independent local critic, local FLUX TV imagery and protected-pixel composition.
- Los Angeles job `701a0a02-4566-407f-890b-63a1194b9707` completed a private machine-reviewed draft. Its generated TV picture was retained. The published caption, headline and chalk menu received an editorial pass.
- Austin job `76e78ca7-51c9-4ec3-9e89-a8e7ef7287b1` correctly stopped after its bounded caption candidates failed checks. Its failure remains in the queue history; it was not relabeled successful. The replacement uses an editor-written caption, a newly locally generated TV picture and the approved Barclay-speaking duo plate.
- Both final images were visually inspected; protected scene/cast pixels changed: zero. Colored pixels: zero. No claim of an audience-measured humor score.

## Important automation limits

The source setup and editorial publication in this demonstration were assisted. The two queue inputs were application-validated and inserted using authenticated database administration after the pulled production login credentials failed application authentication. This does not prove Rick's browser submission flow. No credentials were rotated and no sessions were invalidated.

The local worker ran twice in bounded one-job mode. No reboot or recurring Windows task installation was performed for this request. Restart persistence and fully unattended arbitrary-city discovery/publication are not claimed by this release.

`scripts/automation/city-demo-sources.json` records the two verified article configurations. Runtime v10 has these installed. Publication rendering can be reproduced with `scripts/prepare-city-editions.mjs <runtime-root> <state-root>` from the repository. Retained worker reports are never rewritten by that script.

## Checks

TypeScript, original-collection hash/ZIP tests, worker tests, city metadata/PNG checks and primary article retrieval tests. Reader-facing download copy explicitly distinguishes the original 38-image ZIP from the two new individual PNG downloads.
